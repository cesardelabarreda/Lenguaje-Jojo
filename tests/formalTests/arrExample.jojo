public stand jojo() {
	@@@	
		int MAXN;
		int primes[10], f[10], pc;
		int i;
		int suma;
	@@@
	MAXN = 10;
	i = 0;
	while(i < MAXN) {
		primes[i] = i;
		i = 
			i + 1;
	}
	i = 0;
	while(i < MAXN) {
		suma = suma + primes[i];
		i = i + 1;
	}

	prints(suma);

	prints(f[0]);

	prints(f[5]);
	f[5] = 1 + 3;
	prints(f[0]);
	prints(f[5]);
}