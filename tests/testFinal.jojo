
class chef {
	@@@
		public int chefesoide;
		public string nombre;
		bool boleano;
		private real valorImportante;
		private bool veryVeryImportante;
	@@@

	func string funcionConString(int v) {
		@@@
			string s;
		@@@

		s = "";
		while (v > 0 + 0 * 0) {
			s = s + "a";
			v = v - 1;
		}

		zadust s;
	}

	func public void chef() {
		chefesoide = (((8) * (1 + 15)));
		nombre = "Dio";
	}

	func public real getValorImportante() {
		zadust valorImportante;
	}

	func public stand setValorImportante(bool valorImportante1) {
		veryVeryImportante = valorImportante1;
	}

	
}

class chefe hamon chef {
	@@@
		public int ultimo;
	@@@
}

@@@
	int varGlobal1;
	bool varGlobal2;
@@@

func stand sumas(ref int v1, ref real v2) {
	@@@
		real v3;
	@@@

	while (v3 < 30 && true || (false) && !(!(!!false)) && 15 <= 51 || !(v1 != v2))  {
		v3 = v1 + v2;
		v1 = v2;
		v2 = v3;
	}

	if (v3 - 2 == 1) {
		v3 = v3 - 1;
	}
	else if (v3 + 3 == 2) {
		v3 = v3 - 2;
	}
	else {
		v3 = v3 - 3;
	}

	v2 = v3;
}


func real min(real v1, real v2) {
	if (v1 < v2) {
		zadust v1;
	}
	zadust v2;
}

func int trabajaTodo() {
	@@@
		chefe c;
	@@@


}



public stand jojo() {
	@@@
		int v1, v1_1;
		real v2;
		chefe v5;
		bool arr[5];
		int ind;
	@@@

	ind = 0;
	
	while (ind < 5) {
		arr[ind] = false;
		ind = ind + 1;
	}
	arr[3] = true;



	gets_int(v1);
	gets_real(v2);

	sumas(v1, v2);

	prints_int(v1);
	prints_real(v2);

	v5.chef();
	prints_string(v5.nombre);
	prints_real(v5.setValorImportante());


	ind = 0;

	while (ind < 5) {
		prints_bool(arr[ind]);
		ind = ind + 1;
	}


	zadust;
}